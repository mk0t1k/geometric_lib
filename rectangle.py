def area(a, b): 
    assert a > 0 and b > 0, 'semanticly sides must be positive'
    return a * b 

def perimeter(a, b): 
    assert a > 0 and b > 0, 'semanticly sides must be positive'
    return a + b